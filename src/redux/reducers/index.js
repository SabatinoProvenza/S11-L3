const initialState = {
  prefs: {
    content: [],
  },
}

const mainReducer = function (currentState = initialState, action) {
  switch (action.type) {
    // case

    default:
      return currentState
  }
}

export default mainReducer
